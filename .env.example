# ===========================================
# AUTONOMOUS TRADING BOT - ENVIRONMENT CONFIG
# ===========================================
# Copy this to .env and fill in your values
# For Railway: Set these in the dashboard, not in .env file

# -------------------------------------------
# OKX API CREDENTIALS (Required)
# -------------------------------------------
# Get these from: okx.com -> Profile -> API Management
# IMPORTANT: Only enable "Read" and "Trade" permissions
# NEVER enable "Withdraw" permission!

# Option 1: Plain text (for local development only)
OKX_API_KEY=your_api_key_here
OKX_SECRET_KEY=your_secret_key_here
OKX_PASSPHRASE=your_passphrase_here

# Option 2: Encrypted (for production)
# Generate encryption key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Then encrypt your keys using: python -m src.security.vault
# ENCRYPTION_KEY=your_fernet_key_here
# OKX_API_KEY_ENCRYPTED=encrypted_api_key
# OKX_SECRET_ENCRYPTED=encrypted_secret
# OKX_PASSPHRASE_ENCRYPTED=encrypted_passphrase

# -------------------------------------------
# TRADING MODE
# -------------------------------------------
# "live" = real trading with real money
# "demo" = paper trading (test without real money)
TRADING_MODE=demo

# -------------------------------------------
# TRADING PAIRS
# -------------------------------------------
# Comma-separated list of pairs to trade
TRADING_PAIRS=BTC-USDT,ETH-USDT,SOL-USDT

# -------------------------------------------
# RISK PARAMETERS
# -------------------------------------------
# Maximum position size as % of portfolio
MAX_POSITION_PERCENT=25

# Stop-loss: sell if position is down this %
STOP_LOSS_PERCENT=8

# Take-profit: sell if position is up this %
TAKE_PROFIT_PERCENT=20

# Circuit breaker: pause trading if daily loss exceeds this %
DAILY_LOSS_LIMIT=12

# Maximum concurrent positions
MAX_OPEN_POSITIONS=3

# Minimum AI confidence to execute a trade (0.0 to 1.0)
MIN_CONFIDENCE=0.70

# -------------------------------------------
# TIMING
# -------------------------------------------
# Minutes between analysis cycles
ANALYSIS_INTERVAL=30

# Days of historical data for model training
LOOKBACK_DAYS=90

# -------------------------------------------
# WEB DASHBOARD (Remote Access)
# -------------------------------------------
# Username and password to access the dashboard
# CHANGE THESE! Anyone with these can see your bot status
DASHBOARD_USER=sveerasamy55@gmail.com
DASHBOARD_PASS=senthil12
